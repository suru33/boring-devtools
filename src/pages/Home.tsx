import { Text } from "@mantine/core";

const Home = () => {
  return(
    <div>
      <Text size="xl" weight={700}>
        Select tool from the list
      </Text>
    </div>
  );
};

export default Home;
