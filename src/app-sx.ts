import { Sx } from "@mantine/core";

export const defaultMargin: Sx = {
  marginTop: 15
};

export const verticalGroupIndent: Sx = {
  marginLeft: 20,
  marginTop: 4
};

export const textAreaDefaultRows = 20;
export const defaultTooltipWidth = 280;
